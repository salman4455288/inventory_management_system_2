<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Base color is white, but we tint it dynamically in the layout -->
    <solid android:color="#FFFFFF" />

    <!-- Optional: Add size if you want a fixed default size -->
    <size
        android:width="40dp"
        android:height="40dp" />
</shape>